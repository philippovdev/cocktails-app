import { describe, it } from 'vitest';

describe('cocktails view', () => {
  it.todo('uses pinia state management', () => {});
  it.todo(
    'by default, the first menu item is active and is used for the main page',
    () => {}
  );
  it.todo(
    "root site directory '/' has to redirect user to first menu item",
    () => {}
  );
  it.todo('non-existing pages should redirect to 404 page', () => {});
  it.todo('uses lazy loading for images', () => {});
  it.todo(
    'responsive interface with max width of 1024px and min width of 360px',
    () => {}
  );
  it.todo('active menu item is highlighted', () => {});
  it.todo('handle errors exceptions', () => {});
  it.todo('fetches data', () => {});
  it.todo(
    'every menu item is leading to related page with description',
    () => {}
  );
  it.todo('cocktails code are used for router slugs and menu items', () => {});
  it.todo(
    'uses GET: https://www.thecocktaildb.com/api/json/v1/1/search.php?s=<cocktail_code>',
    () => {}
  );
  it.todo('cocktails to use: margarita, mojito, a1, kir', () => {});
});
